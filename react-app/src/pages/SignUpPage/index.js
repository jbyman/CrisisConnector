import React from 'react';

import Page from 'components/Page';

const SignUpPage = ({ className }) => {
  return <Page className={className}>{'Hello, from the sign up page!'}</Page>;
};

export default SignUpPage;
